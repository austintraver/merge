{
  "scripts": {
    "build": "npx web-ext build --config 'web-ext-config.js' --overwrite-dest --artifacts-dir '.'"
  },
  "devDependencies": {
    "@types/chrome": "0.0.136",
    "typescript": "^4.2.4",
    "web-ext": "^6.1.0",
    "webextension-polyfill": "^0.8.0",
    "webextension-polyfill-ts": "^0.25.0"
  }
}
